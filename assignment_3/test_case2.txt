%% 
    integer   i, max, sum;
	integer a,b,c;
	
    if(a > b) 
		a:= a+c;
	else
		a:= a-b;
	fi

    sum := 0;
    i := 1;
    read ( max);
    while (i <  max) {
          sum := sum + i;
          i  := i + 1; 
     }
     write (sum+max);
