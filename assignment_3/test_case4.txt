%% 
    integer   i, max, sum;
	boolean	a,b;

	a := 1;
    sum := 0;
    i := 1;
    read ( max);
    while (i <  max) do {
          sum := sum + i;
          i  := i + 1; 
     }
     write (sum+max);
